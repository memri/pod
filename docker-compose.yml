version: '3'
services:
    dgraph:
      image: dgraph/standalone:latest
      ports:
        - "8000:8000"
        - "8080:8080"
        - "9080:9080"
      networks:
        - my-net

    pod:
      build:
        context: .
        dockerfile: Dockerfile
      restart: always
      environment:
        - SCHEMA_DROP=false
        - SCHEMA_SET=true
        - DGRAPH_HOST=pod_dgraph_1
        - RUST_LOG=trace
      image: pod:latest
      ports:
        - "3030:3030"
      depends_on:
        - dgraph
      networks:
        - my-net
networks:
  my-net:
